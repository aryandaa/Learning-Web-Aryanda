{"id":"cybersecurity/forensic/tools/dd-dan-dcfldd","title":"Dd dan Dcfldd","slug":"dd dan dcfldd","relativePath":"CyberSecurity/Forensic/Tools/Dd dan Dcfldd.md","folder":"CyberSecurity/Forensic/Tools","frontmatter":{"tags":[],"aliases":[]},"tags":["tool"],"aliases":[],"headings":[{"depth":2,"text":"1. Instalasi","id":"1-instalasi"},{"depth":1,"text":"2. Penggunaan Dasar `dd`","id":"2-penggunaan-dasar-dd"},{"depth":1,"text":"3. Flag Penting `dd`","id":"3-flag-penting-dd"},{"depth":1,"text":"4. Contoh Imaging yang Benar","id":"4-contoh-imaging-yang-benar"},{"depth":1,"text":"5. Mengambil Satu Partition dari Image","id":"5-mengambil-satu-partition-dari-image"},{"depth":1,"text":"6. `dcfldd`: dd dengan Hash Terintegrasi","id":"6-dcfldd-dd-dengan-hash-terintegrasi"},{"depth":1,"text":"7. `dc3dd`: Versi yang Lebih Forensic","id":"7-dc3dd-versi-yang-lebih-forensic"},{"depth":1,"text":"8. Format Image Lain: E01","id":"8-format-image-lain-e01"},{"depth":1,"text":"9. Posisi Acquisition dalam Workflow","id":"9-posisi-acquisition-dalam-workflow"},{"depth":1,"text":"10. Command yang Perlu Kamu Kuasai","id":"10-command-yang-perlu-kamu-kuasai"}],"content":"#tool \n\nSetelah mengidentifikasi evidence dengan `lsblk` dan memastikan integrity dengan hash, langkah berikutnya adalah **acquisition**, yaitu membuat salinan forensik dari evidence. Tool utama untuk ini adalah `dd`, dan versi forensic-nya: `dcfldd` dan `dc3dd`.\n\nPenting untuk dipahami sejak awal: dalam forensic, kita **tidak menganalisis evidence asli**. Kita membuat image, lalu menganalisis image tersebut. Image adalah salinan bit-for-bit dari media penyimpanan.\n\n```\nMedia penyimpanan /dev/sdb\n    ↓\ndd / dcfldd\n    ↓\nevidence.img (salinan bit-for-bit)\n    ↓\nAnalisis terhadap image, bukan media asli\n```\n\n---\n## 1. Instalasi\n\n`dd` berasal dari `coreutils`, sudah pasti tersedia.\n\n```bash\ndd --version\n```\n\n`dcfldd` dan `dc3dd` perlu diinstall:\n\n```bash\nsudo apt update\nsudo apt install dcfldd dc3dd\n```\n\nVerifikasi:\n\n```bash\ndcfldd --version\n```\n\n```bash\ndc3dd --version\n```\n\n---\n# 2. Penggunaan Dasar `dd`\n\nSyntax `dd` berbeda dari command biasa. Ia menggunakan format `if=` (input file) dan `of=` (output file):\n\n```bash\nsudo dd if=/dev/sdb of=evidence.img\n```\n\nIni menyalin seluruh isi `/dev/sdb` ke `evidence.img`.\n\nContoh lain yang sering dipakai:\n\n```bash\nsudo dd if=/dev/sdb1 of=partition1.img bs=4M status=progress\n```\n\nPerhatikan `bs=4M`. Ini adalah block size, menentukan berapa banyak data dibaca sekaligus. Untuk disk besar, block size yang lebih besar membuat proses lebih cepat.\n\n`status=progress` menampilkan progres transfer.\n\n```\nSebelum menjalankan dd, pastikan dengan lsblk bahwa kamu menyalin device\nyang benar. Satu kesalahan penulisan if/of bisa menghancurkan evidence\natau menghapus data di mesinmu sendiri.\n```\n\n---\n# 3. Flag Penting `dd`\n\n```bash\nbs=<ukuran>\n```\n\nblock size, misalnya `bs=4M`.\n\n```bash\ncount=<jumlah>\n```\n\njumlah block yang disalin. Berguna untuk mengambil sebagian image (misalnya untuk triage cepat).\n\n```bash\nskip=<jumlah>\n```\n\nmelewati sejumlah block pada input. Berguna untuk mengambil partition tertentu dari image tanpa membaca seluruh disk.\n\n```bash\nseek=<jumlah>\n```\n\nmelewati sejumlah block pada output.\n\n```bash\niflag=fullblock\n```\n\nmemastikan setiap operasi membaca block penuh. Penting saat membaca dari pipe atau device.\n\n```bash\nconv=noerror,sync\n```\n\nini adalah kombinasi paling penting untuk forensic:\n\n```text\nnoerror  → terus lanjut meskipun ada error baca\nsync     → isi block yang gagal dibaca dengan null byte\n```\n\nTanpa `noerror,sync`, satu sektor rusak bisa menghentikan seluruh proses imaging. Dengan kombinasi ini, image tetap terbentuk dan posisi data tetap selaras, hanya bagian yang rusak yang terisi byte nol.\n\n---\n# 4. Contoh Imaging yang Benar\n\n```bash\nsudo dd if=/dev/sdb of=evidence.img bs=4M conv=noerror,sync iflag=fullblock status=progress\n```\n\nSetelah selesai, verifikasi:\n\n```bash\nsha256sum /dev/sdb evidence.img\n```\n\nPerlu diperhatikan: menghitung hash `/dev/sdb` langsung membutuhkan membaca seluruh device. Ini normal dan sah dilakukan selama device tidak sedang berubah.\n\n---\n# 5. Mengambil Satu Partition dari Image\n\nJika kamu punya image penuh dan hanya butuh satu partition:\n\n```bash\nfdisk -l evidence.img\n```\n\nmelihat daftar partition dan offsetnya. Kemudian:\n\n```bash\nsudo dd if=evidence.img of=partition2.img bs=512 skip=<offset_partition> count=<jumlah_sector>\n```\n\n`bs=512` dipakai karena offset partition biasanya dihitung dalam sector 512 byte. `skip` menentukan mulai dari sector mana, `count` menentukan berapa sector.\n\nCara ini berguna ketika kamu tidak ingin menganalisis seluruh disk.\n\n---\n# 6. `dcfldd`: dd dengan Hash Terintegrasi\n\n`dcfldd` adalah versi forensic dari `dd`. Keunggulannya: **menghitung hash sambil menyalin**, sehingga tidak perlu membaca data dua kali.\n\n```bash\nsudo dcfldd if=/dev/sdb of=evidence.img bs=4M hash=sha256 hashlog=hash.txt status=on\n```\n\nOutput:\n\n```text\n blocks (X bytes) copied.\nSHA256 (evidence.img) = a1b2c3...\n```\n\nFlag utama `dcfldd`:\n\n```bash\nhash=sha256\n```\n\nalgoritma hash yang dihitung.\n\n```bash\nhashlog=<file>\n```\n\nmenyimpan hash ke file.\n\n```bash\nhashwindow=<jumlah>\n```\n\nmenghitung hash per blok interval (untuk verifikasi bertahap).\n\n```bash\nstatus=on\n```\n\nmenampilkan progres.\n\n```bash\nvf=<file>\n```\n\nmemverifikasi output terhadap file hash.\n\nContoh verifikasi:\n\n```bash\ndcfldd if=evidence.img vf=hash.txt\n```\n\n---\n# 7. `dc3dd`: Versi yang Lebih Forensic\n\n`dc3dd` adalah pengembangan dari `dcfldd` yang menambahkan fitur logging yang lebih baik:\n\n```bash\nsudo dc3dd if=/dev/sdb of=evidence.img bs=4M hash=sha256 hashlog=hash.txt log=acquisition.log status=on\n```\n\nKeunggulan `dc3dd`:\n\n```bash\nlog=<file>\n```\n\nmenyimpan catatan seluruh proses (command, waktu, error) ke file log. Log ini bisa menjadi bagian dari chain of custody.\n\n```bash\nofsz=<ukuran>\n```\n\nmembatasi ukuran output (berguna untuk membagi image menjadi beberapa bagian).\n\n```bash\nerrlog=<file>\n```\n\nmenyimpan error baca secara terpisah.\n\n```bash\ncount=<jumlah>\n```\n\n```bash\nskip=<jumlah>\n```\n\nsama seperti `dd`.\n\n---\n\n# 8. Format Image Lain: E01\n\n`dd` menghasilkan image mentah (raw). Format lain yang sering dipakai di dunia forensic adalah **E01** (EnCase Evidence File) yang menyimpan metadata dan hash di dalam file image-nya sendiri.\n\nTool untuk membuat E01 adalah `ewfacquire` dari package `libewf`:\n\n```bash\nsudo apt install ewf-tools\n```\n\n```bash\nsudo ewfacquire /dev/sdb -t evidence\n```\n\nHasilnya berupa file dengan ekstensi `.E01` (atau `.E02`, `.E03` jika terbagi).\n\nUntuk membuka E01:\n\n```bash\newfmount evidence.E01 mountpoint/\n```\n\natau langsung dengan `ewfexport`:\n\n```bash\newfexport evidence.E01 -t exported\n```\n\nDi CTF, format raw (`img`, `dd`) lebih sering muncul karena lebih sederhana. Tetapi di dunia nyata, E01 adalah standar.\n\n---\n# 9. Posisi Acquisition dalam Workflow\n\n```text\nlsblk\n ↓\nIdentifikasi device evidence\n ↓\ndd / dcfldd / dc3dd\n ↓\nImage + hash\n ↓\nVerifikasi hash\n ↓\nAnalisis image\n```\n\nSelalu catat: device apa yang di-image, kapan, dengan command apa, dan berapa hash hasilnya. Informasi ini yang membuat image dapat dipertanggungjawabkan.\n\n---\n# 10. Command yang Perlu Kamu Kuasai\n\n```bash\nsudo dd if=/dev/sdb of=evidence.img bs=4M conv=noerror,sync status=progress\n```\n\n```bash\nsudo dcfldd if=/dev/sdb of=evidence.img bs=4M hash=sha256 hashlog=hash.txt status=on\n```\n\n```bash\nsudo dc3dd if=/dev/sdb of=evidence.img bs=4M hash=sha256 hashlog=hash.txt log=acq.log status=on\n```\n\n```bash\ndcfldd if=evidence.img vf=hash.txt\n```\n\n```bash\nsha256sum /dev/sdb evidence.img\n```\n","excerpt":"#tool","readingTime":5,"updated":null,"contentHash":"sha256:526bab0afd2beb5863c5329c1512d34dfec4fe052c1dbb16a661e2701084a840","outputPath":"CyberSecurity/Forensic/Tools/Dd dan Dcfldd","breadcrumb":["CyberSecurity","Forensic","Tools"],"links":[],"backlinks":[{"id":"cybersecurity/forensic/tools/tools-forensic","title":"Tools Forensic"}],"brokenLinks":[],"html":"<p>Setelah mengidentifikasi evidence dengan <code>lsblk</code> dan memastikan integrity dengan hash, langkah berikutnya adalah <strong>acquisition</strong>, yaitu membuat salinan forensik dari evidence. Tool utama untuk ini adalah <code>dd</code>, dan versi forensic-nya: <code>dcfldd</code> dan <code>dc3dd</code>.</p>\n<p>Penting untuk dipahami sejak awal: dalam forensic, kita <strong>tidak menganalisis evidence asli</strong>. Kita membuat image, lalu menganalisis image tersebut. Image adalah salinan bit-for-bit dari media penyimpanan.</p>\n<pre><code>Media penyimpanan /dev/sdb\n    ↓\ndd / dcfldd\n    ↓\nevidence.img (salinan bit-for-bit)\n    ↓\nAnalisis terhadap image, bukan media asli\n</code></pre>\n<hr>\n<h2 id=\"1-instalasi\">1. Instalasi</h2>\n<p><code>dd</code> berasal dari <code>coreutils</code>, sudah pasti tersedia.</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">dd</span> --version\n</code></pre>\n<p><code>dcfldd</code> dan <code>dc3dd</code> perlu diinstall:</p>\n<pre><code class=\"hljs language-bash\">sudo apt update\nsudo apt install dcfldd dc3dd\n</code></pre>\n<p>Verifikasi:</p>\n<pre><code class=\"hljs language-bash\">dcfldd --version\n</code></pre>\n<pre><code class=\"hljs language-bash\">dc3dd --version\n</code></pre>\n<hr>\n<h1 id=\"2-penggunaan-dasar-dd\">2. Penggunaan Dasar <code>dd</code></h1>\n<p>Syntax <code>dd</code> berbeda dari command biasa. Ia menggunakan format <code>if=</code> (input file) dan <code>of=</code> (output file):</p>\n<pre><code class=\"hljs language-bash\">sudo <span class=\"hljs-built_in\">dd</span> <span class=\"hljs-keyword\">if</span>=/dev/sdb of=evidence.img\n</code></pre>\n<p>Ini menyalin seluruh isi <code>/dev/sdb</code> ke <code>evidence.img</code>.</p>\n<p>Contoh lain yang sering dipakai:</p>\n<pre><code class=\"hljs language-bash\">sudo <span class=\"hljs-built_in\">dd</span> <span class=\"hljs-keyword\">if</span>=/dev/sdb1 of=partition1.img bs=4M status=progress\n</code></pre>\n<p>Perhatikan <code>bs=4M</code>. Ini adalah block size, menentukan berapa banyak data dibaca sekaligus. Untuk disk besar, block size yang lebih besar membuat proses lebih cepat.</p>\n<p><code>status=progress</code> menampilkan progres transfer.</p>\n<pre><code>Sebelum menjalankan dd, pastikan dengan lsblk bahwa kamu menyalin device\nyang benar. Satu kesalahan penulisan if/of bisa menghancurkan evidence\natau menghapus data di mesinmu sendiri.\n</code></pre>\n<hr>\n<h1 id=\"3-flag-penting-dd\">3. Flag Penting <code>dd</code></h1>\n<pre><code class=\"hljs language-bash\">bs=&#x3C;ukuran>\n</code></pre>\n<p>block size, misalnya <code>bs=4M</code>.</p>\n<pre><code class=\"hljs language-bash\">count=&#x3C;jumlah>\n</code></pre>\n<p>jumlah block yang disalin. Berguna untuk mengambil sebagian image (misalnya untuk triage cepat).</p>\n<pre><code class=\"hljs language-bash\">skip=&#x3C;jumlah>\n</code></pre>\n<p>melewati sejumlah block pada input. Berguna untuk mengambil partition tertentu dari image tanpa membaca seluruh disk.</p>\n<pre><code class=\"hljs language-bash\">seek=&#x3C;jumlah>\n</code></pre>\n<p>melewati sejumlah block pada output.</p>\n<pre><code class=\"hljs language-bash\">iflag=fullblock\n</code></pre>\n<p>memastikan setiap operasi membaca block penuh. Penting saat membaca dari pipe atau device.</p>\n<pre><code class=\"hljs language-bash\">conv=noerror,<span class=\"hljs-built_in\">sync</span>\n</code></pre>\n<p>ini adalah kombinasi paling penting untuk forensic:</p>\n<pre><code class=\"hljs language-text\">noerror  → terus lanjut meskipun ada error baca\nsync     → isi block yang gagal dibaca dengan null byte\n</code></pre>\n<p>Tanpa <code>noerror,sync</code>, satu sektor rusak bisa menghentikan seluruh proses imaging. Dengan kombinasi ini, image tetap terbentuk dan posisi data tetap selaras, hanya bagian yang rusak yang terisi byte nol.</p>\n<hr>\n<h1 id=\"4-contoh-imaging-yang-benar\">4. Contoh Imaging yang Benar</h1>\n<pre><code class=\"hljs language-bash\">sudo <span class=\"hljs-built_in\">dd</span> <span class=\"hljs-keyword\">if</span>=/dev/sdb of=evidence.img bs=4M conv=noerror,<span class=\"hljs-built_in\">sync</span> iflag=fullblock status=progress\n</code></pre>\n<p>Setelah selesai, verifikasi:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">sha256sum</span> /dev/sdb evidence.img\n</code></pre>\n<p>Perlu diperhatikan: menghitung hash <code>/dev/sdb</code> langsung membutuhkan membaca seluruh device. Ini normal dan sah dilakukan selama device tidak sedang berubah.</p>\n<hr>\n<h1 id=\"5-mengambil-satu-partition-dari-image\">5. Mengambil Satu Partition dari Image</h1>\n<p>Jika kamu punya image penuh dan hanya butuh satu partition:</p>\n<pre><code class=\"hljs language-bash\">fdisk -l evidence.img\n</code></pre>\n<p>melihat daftar partition dan offsetnya. Kemudian:</p>\n<pre><code class=\"hljs language-bash\">sudo <span class=\"hljs-built_in\">dd</span> <span class=\"hljs-keyword\">if</span>=evidence.img of=partition2.img bs=512 skip=&#x3C;offset_partition> count=&#x3C;jumlah_sector>\n</code></pre>\n<p><code>bs=512</code> dipakai karena offset partition biasanya dihitung dalam sector 512 byte. <code>skip</code> menentukan mulai dari sector mana, <code>count</code> menentukan berapa sector.</p>\n<p>Cara ini berguna ketika kamu tidak ingin menganalisis seluruh disk.</p>\n<hr>\n<h1 id=\"6-dcfldd-dd-dengan-hash-terintegrasi\">6. <code>dcfldd</code>: dd dengan Hash Terintegrasi</h1>\n<p><code>dcfldd</code> adalah versi forensic dari <code>dd</code>. Keunggulannya: <strong>menghitung hash sambil menyalin</strong>, sehingga tidak perlu membaca data dua kali.</p>\n<pre><code class=\"hljs language-bash\">sudo dcfldd <span class=\"hljs-keyword\">if</span>=/dev/sdb of=evidence.img bs=4M <span class=\"hljs-built_in\">hash</span>=sha256 hashlog=hash.txt status=on\n</code></pre>\n<p>Output:</p>\n<pre><code class=\"hljs language-text\"> blocks (X bytes) copied.\nSHA256 (evidence.img) = a1b2c3...\n</code></pre>\n<p>Flag utama <code>dcfldd</code>:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">hash</span>=sha256\n</code></pre>\n<p>algoritma hash yang dihitung.</p>\n<pre><code class=\"hljs language-bash\">hashlog=&#x3C;file>\n</code></pre>\n<p>menyimpan hash ke file.</p>\n<pre><code class=\"hljs language-bash\">hashwindow=&#x3C;jumlah>\n</code></pre>\n<p>menghitung hash per blok interval (untuk verifikasi bertahap).</p>\n<pre><code class=\"hljs language-bash\">status=on\n</code></pre>\n<p>menampilkan progres.</p>\n<pre><code class=\"hljs language-bash\">vf=&#x3C;file>\n</code></pre>\n<p>memverifikasi output terhadap file hash.</p>\n<p>Contoh verifikasi:</p>\n<pre><code class=\"hljs language-bash\">dcfldd <span class=\"hljs-keyword\">if</span>=evidence.img vf=hash.txt\n</code></pre>\n<hr>\n<h1 id=\"7-dc3dd-versi-yang-lebih-forensic\">7. <code>dc3dd</code>: Versi yang Lebih Forensic</h1>\n<p><code>dc3dd</code> adalah pengembangan dari <code>dcfldd</code> yang menambahkan fitur logging yang lebih baik:</p>\n<pre><code class=\"hljs language-bash\">sudo dc3dd <span class=\"hljs-keyword\">if</span>=/dev/sdb of=evidence.img bs=4M <span class=\"hljs-built_in\">hash</span>=sha256 hashlog=hash.txt <span class=\"hljs-built_in\">log</span>=acquisition.log status=on\n</code></pre>\n<p>Keunggulan <code>dc3dd</code>:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">log</span>=&#x3C;file>\n</code></pre>\n<p>menyimpan catatan seluruh proses (command, waktu, error) ke file log. Log ini bisa menjadi bagian dari chain of custody.</p>\n<pre><code class=\"hljs language-bash\">ofsz=&#x3C;ukuran>\n</code></pre>\n<p>membatasi ukuran output (berguna untuk membagi image menjadi beberapa bagian).</p>\n<pre><code class=\"hljs language-bash\">errlog=&#x3C;file>\n</code></pre>\n<p>menyimpan error baca secara terpisah.</p>\n<pre><code class=\"hljs language-bash\">count=&#x3C;jumlah>\n</code></pre>\n<pre><code class=\"hljs language-bash\">skip=&#x3C;jumlah>\n</code></pre>\n<p>sama seperti <code>dd</code>.</p>\n<hr>\n<h1 id=\"8-format-image-lain-e01\">8. Format Image Lain: E01</h1>\n<p><code>dd</code> menghasilkan image mentah (raw). Format lain yang sering dipakai di dunia forensic adalah <strong>E01</strong> (EnCase Evidence File) yang menyimpan metadata dan hash di dalam file image-nya sendiri.</p>\n<p>Tool untuk membuat E01 adalah <code>ewfacquire</code> dari package <code>libewf</code>:</p>\n<pre><code class=\"hljs language-bash\">sudo apt install ewf-tools\n</code></pre>\n<pre><code class=\"hljs language-bash\">sudo ewfacquire /dev/sdb -t evidence\n</code></pre>\n<p>Hasilnya berupa file dengan ekstensi <code>.E01</code> (atau <code>.E02</code>, <code>.E03</code> jika terbagi).</p>\n<p>Untuk membuka E01:</p>\n<pre><code class=\"hljs language-bash\">ewfmount evidence.E01 mountpoint/\n</code></pre>\n<p>atau langsung dengan <code>ewfexport</code>:</p>\n<pre><code class=\"hljs language-bash\">ewfexport evidence.E01 -t exported\n</code></pre>\n<p>Di CTF, format raw (<code>img</code>, <code>dd</code>) lebih sering muncul karena lebih sederhana. Tetapi di dunia nyata, E01 adalah standar.</p>\n<hr>\n<h1 id=\"9-posisi-acquisition-dalam-workflow\">9. Posisi Acquisition dalam Workflow</h1>\n<pre><code class=\"hljs language-text\">lsblk\n ↓\nIdentifikasi device evidence\n ↓\ndd / dcfldd / dc3dd\n ↓\nImage + hash\n ↓\nVerifikasi hash\n ↓\nAnalisis image\n</code></pre>\n<p>Selalu catat: device apa yang di-image, kapan, dengan command apa, dan berapa hash hasilnya. Informasi ini yang membuat image dapat dipertanggungjawabkan.</p>\n<hr>\n<h1 id=\"10-command-yang-perlu-kamu-kuasai\">10. Command yang Perlu Kamu Kuasai</h1>\n<pre><code class=\"hljs language-bash\">sudo <span class=\"hljs-built_in\">dd</span> <span class=\"hljs-keyword\">if</span>=/dev/sdb of=evidence.img bs=4M conv=noerror,<span class=\"hljs-built_in\">sync</span> status=progress\n</code></pre>\n<pre><code class=\"hljs language-bash\">sudo dcfldd <span class=\"hljs-keyword\">if</span>=/dev/sdb of=evidence.img bs=4M <span class=\"hljs-built_in\">hash</span>=sha256 hashlog=hash.txt status=on\n</code></pre>\n<pre><code class=\"hljs language-bash\">sudo dc3dd <span class=\"hljs-keyword\">if</span>=/dev/sdb of=evidence.img bs=4M <span class=\"hljs-built_in\">hash</span>=sha256 hashlog=hash.txt <span class=\"hljs-built_in\">log</span>=acq.log status=on\n</code></pre>\n<pre><code class=\"hljs language-bash\">dcfldd <span class=\"hljs-keyword\">if</span>=evidence.img vf=hash.txt\n</code></pre>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">sha256sum</span> /dev/sdb evidence.img\n</code></pre>","previous":"cybersecurity/forensic/tools/bulk-extractor","next":"cybersecurity/forensic/tools/exiftool"}