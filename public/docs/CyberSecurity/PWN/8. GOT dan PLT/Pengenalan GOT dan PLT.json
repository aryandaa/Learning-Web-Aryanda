{"id":"cybersecurity/pwn/8.-got-dan-plt/pengenalan-got-dan-plt","title":"Pengenalan GOT dan PLT","slug":"pengenalan got dan plt","relativePath":"CyberSecurity/PWN/8. GOT dan PLT/Pengenalan GOT dan PLT.md","folder":"CyberSecurity/PWN/8. GOT dan PLT","frontmatter":{"tags":[],"aliases":[]},"tags":["cybersecurity"],"aliases":[],"headings":[{"depth":2,"text":"Alur panggilan fungsi","id":"alur-panggilan-fungsi"},{"depth":2,"text":"Lazy binding","id":"lazy-binding"},{"depth":2,"text":"Kenapa ini penting untuk PWN?","id":"kenapa-ini-penting-untuk-pwn"},{"depth":2,"text":"Lihat PLT & GOT","id":"lihat-plt-got"},{"depth":1,"text":"symbol","id":"symbol"},{"depth":1,"text":"relokasi (GOT)","id":"relokasi-got"},{"depth":1,"text":"disassembly PLT","id":"disassembly-plt"},{"depth":2,"text":"Contoh leak via GOT","id":"contoh-leak-via-got"},{"depth":1,"text":"panggil puts(puts@got) - cetak isi GOT = alamat puts di libc","id":"panggil-putsputsgot-cetak-isi-got-alamat-puts-di-libc"},{"depth":2,"text":"GOT overwrite singkat","id":"got-overwrite-singkat"},{"depth":1,"text":"timpa puts@got dengan system (via format string / overflow)","id":"timpa-putsgot-dengan-system-via-format-string-overflow"},{"depth":2,"text":"Istilah","id":"istilah"},{"depth":2,"text":"Catatan","id":"catatan"}],"content":"#cybersecurity\n\nSaat program dynamic memanggil fungsi libc (misal `puts`), dia tidak langsung tahu alamat puts di libc (karena ASLR). Dia memakai **PLT** dan **GOT**.\n\n## Alur panggilan fungsi\n\n```text\nprogram memanggil puts(\"hi\")\n        |\n        v\nputs@plt  (stub kecil di binary)\n        |\n        v\nputs@got  (tabel berisi alamat puts di libc)\n        |\n        v\nalamat puts di libc (ASLR, diisi dynamic linker)\n```\n\n- **PLT** - potongan kode di binary untuk setiap fungsi yang dipanggil\n- **GOT** - tabel berisi **alamat sebenarnya** fungsi di libc\n\n## Lazy binding\n\nAwalnya `puts@got` menunjuk balik ke PLT (untuk resolve). Saat pertama kali `puts` dipanggil, dynamic linker mencari alamat puts di libc, **mengisi `puts@got`**, lalu eksekusi berjalan normal. Panggilan berikutnya langsung lewat GOT.\n\n## Kenapa ini penting untuk PWN?\n\n1. **Leak** - isi `puts@got` = alamat puts di libc. Kalau kita bisa membuat program mencetak isi GOT (misal `puts(puts@got)`), kita leak libc → hitung base ([[Ret2libc]]).\n2. **GOT overwrite** - kalau GOT writable (Partial RELRO), kita ubah isi `puts@got` jadi `system` → semua panggilan `puts(x)` jadi `system(x)`.\n\n## Lihat PLT & GOT\n\n```bash\n# symbol\nreadelf -s chall | grep -E \"puts|system\"\n\n# relokasi (GOT)\nreadelf -r chall\n\n# disassembly PLT\nobjdump -d chall | grep -A5 \"<puts@plt>\"\n```\n\nDengan pwntools:\n\n```python\nfrom pwn import *\nelf = ELF(\"./chall\")\nelf.plt[\"puts\"]   # alamat stub PLT\nelf.got[\"puts\"]   # alamat entri GOT\n```\n\n## Contoh leak via GOT\n\n```python\n# panggil puts(puts@got) - cetak isi GOT = alamat puts di libc\npayload = b\"A\"*offset + p64(elf.plt.puts) + p64(elf.sym.main) + p64(elf.got.puts)\n```\n\nLihat alur lengkap di [[Ret2libc]].\n\n## GOT overwrite singkat\n\n```python\n# timpa puts@got dengan system (via format string / overflow)\npayload = fmtstr_payload(pos, {elf.got.puts: libc.sym.system})\n```\n\nLengkap di [[GOT Overwrite]].\n\n## Istilah\n\n| Istilah | Arti |\n|---|---|\n| PLT | Stub pemanggil fungsi (di binary) |\n| GOT | Tabel alamat fungsi libc |\n| GOT entry | Satu entri per fungsi |\n| Resolve | Proses mengisi GOT pertama kali |\n| Lazy binding | Resolve saat fungsi pertama dipanggil |\n\n## Catatan\n\n- `puts@plt` → alamat untuk **memanggil** puts.\n- `puts@got` → alamat yang **isinya** alamat puts di libc.\n- GOT writable hanya kalau **Partial RELRO** (lihat [[RELRO]]). Full RELRO → GOT read-only.\n","excerpt":"#cybersecurity","readingTime":2,"updated":null,"contentHash":"sha256:cbbd2329bbbd03113221bc097350dc907000117c9bb96f3069cabd4fc317ad87","outputPath":"CyberSecurity/PWN/8. GOT dan PLT/Pengenalan GOT dan PLT","breadcrumb":["CyberSecurity","PWN","8. GOT dan PLT"],"links":[{"id":"cybersecurity/pwn/5.-rop-(return-oriented-programming)/ret2libc","title":"Ret2libc","href":"/docs/cybersecurity/pwn/5.-rop-(return-oriented-programming)/ret2libc"},{"id":"cybersecurity/pwn/8.-got-dan-plt/got-overwrite","title":"GOT Overwrite","href":"/docs/cybersecurity/pwn/8.-got-dan-plt/got-overwrite"},{"id":"cybersecurity/pwn/3.-mitigations-(proteksi-binary)/relro","title":"RELRO","href":"/docs/cybersecurity/pwn/3.-mitigations-(proteksi-binary)/relro"}],"backlinks":[{"id":"cybersecurity/pwn/8.-got-dan-plt/8.-got-dan-plt","title":"8. GOT dan PLT"}],"brokenLinks":[],"html":"<p>Saat program dynamic memanggil fungsi libc (misal <code>puts</code>), dia tidak langsung tahu alamat puts di libc (karena ASLR). Dia memakai <strong>PLT</strong> dan <strong>GOT</strong>.</p>\n<h2 id=\"alur-panggilan-fungsi\">Alur panggilan fungsi</h2>\n<pre><code class=\"hljs language-text\">program memanggil puts(\"hi\")\n        |\n        v\nputs@plt  (stub kecil di binary)\n        |\n        v\nputs@got  (tabel berisi alamat puts di libc)\n        |\n        v\nalamat puts di libc (ASLR, diisi dynamic linker)\n</code></pre>\n<ul>\n<li><strong>PLT</strong> - potongan kode di binary untuk setiap fungsi yang dipanggil</li>\n<li><strong>GOT</strong> - tabel berisi <strong>alamat sebenarnya</strong> fungsi di libc</li>\n</ul>\n<h2 id=\"lazy-binding\">Lazy binding</h2>\n<p>Awalnya <code>puts@got</code> menunjuk balik ke PLT (untuk resolve). Saat pertama kali <code>puts</code> dipanggil, dynamic linker mencari alamat puts di libc, <strong>mengisi <code>puts@got</code></strong>, lalu eksekusi berjalan normal. Panggilan berikutnya langsung lewat GOT.</p>\n<h2 id=\"kenapa-ini-penting-untuk-pwn\">Kenapa ini penting untuk PWN?</h2>\n<ol>\n<li><strong>Leak</strong> - isi <code>puts@got</code> = alamat puts di libc. Kalau kita bisa membuat program mencetak isi GOT (misal <code>puts(puts@got)</code>), kita leak libc → hitung base (<a href=\"/docs/cybersecurity/pwn/5.-rop-(return-oriented-programming)/ret2libc\" class=\"wiki-link\">Ret2libc</a>).</li>\n<li><strong>GOT overwrite</strong> - kalau GOT writable (Partial RELRO), kita ubah isi <code>puts@got</code> jadi <code>system</code> → semua panggilan <code>puts(x)</code> jadi <code>system(x)</code>.</li>\n</ol>\n<h2 id=\"lihat-plt-got\">Lihat PLT &#x26; GOT</h2>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-comment\"># symbol</span>\nreadelf -s chall | grep -E <span class=\"hljs-string\">\"puts|system\"</span>\n\n<span class=\"hljs-comment\"># relokasi (GOT)</span>\nreadelf -r chall\n\n<span class=\"hljs-comment\"># disassembly PLT</span>\nobjdump -d chall | grep -A5 <span class=\"hljs-string\">\"&#x3C;puts@plt>\"</span>\n</code></pre>\n<p>Dengan pwntools:</p>\n<pre><code class=\"hljs language-python\"><span class=\"hljs-keyword\">from</span> pwn <span class=\"hljs-keyword\">import</span> *\nelf = ELF(<span class=\"hljs-string\">\"./chall\"</span>)\nelf.plt[<span class=\"hljs-string\">\"puts\"</span>]   <span class=\"hljs-comment\"># alamat stub PLT</span>\nelf.got[<span class=\"hljs-string\">\"puts\"</span>]   <span class=\"hljs-comment\"># alamat entri GOT</span>\n</code></pre>\n<h2 id=\"contoh-leak-via-got\">Contoh leak via GOT</h2>\n<pre><code class=\"hljs language-python\"><span class=\"hljs-comment\"># panggil puts(puts@got) - cetak isi GOT = alamat puts di libc</span>\npayload = <span class=\"hljs-string\">b\"A\"</span>*offset + p64(elf.plt.puts) + p64(elf.sym.main) + p64(elf.got.puts)\n</code></pre>\n<p>Lihat alur lengkap di <a href=\"/docs/cybersecurity/pwn/5.-rop-(return-oriented-programming)/ret2libc\" class=\"wiki-link\">Ret2libc</a>.</p>\n<h2 id=\"got-overwrite-singkat\">GOT overwrite singkat</h2>\n<pre><code class=\"hljs language-python\"><span class=\"hljs-comment\"># timpa puts@got dengan system (via format string / overflow)</span>\npayload = fmtstr_payload(pos, {elf.got.puts: libc.sym.system})\n</code></pre>\n<p>Lengkap di <a href=\"/docs/cybersecurity/pwn/8.-got-dan-plt/got-overwrite\" class=\"wiki-link\">GOT Overwrite</a>.</p>\n<h2 id=\"istilah\">Istilah</h2>\n<table>\n<thead>\n<tr>\n<th>Istilah</th>\n<th>Arti</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>PLT</td>\n<td>Stub pemanggil fungsi (di binary)</td>\n</tr>\n<tr>\n<td>GOT</td>\n<td>Tabel alamat fungsi libc</td>\n</tr>\n<tr>\n<td>GOT entry</td>\n<td>Satu entri per fungsi</td>\n</tr>\n<tr>\n<td>Resolve</td>\n<td>Proses mengisi GOT pertama kali</td>\n</tr>\n<tr>\n<td>Lazy binding</td>\n<td>Resolve saat fungsi pertama dipanggil</td>\n</tr>\n</tbody>\n</table>\n<h2 id=\"catatan\">Catatan</h2>\n<ul>\n<li><code>puts@plt</code> → alamat untuk <strong>memanggil</strong> puts.</li>\n<li><code>puts@got</code> → alamat yang <strong>isinya</strong> alamat puts di libc.</li>\n<li>GOT writable hanya kalau <strong>Partial RELRO</strong> (lihat <a href=\"/docs/cybersecurity/pwn/3.-mitigations-(proteksi-binary)/relro\" class=\"wiki-link\">RELRO</a>). Full RELRO → GOT read-only.</li>\n</ul>","previous":"cybersecurity/pwn/8.-got-dan-plt/got-overwrite","next":"cybersecurity/pwn/9.-integer-overflow/9.-integer-overflow"}