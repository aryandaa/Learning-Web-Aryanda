{"id":"pemrograman/javascript/javascript-for-frontend/onload","title":"Onload","slug":"onload","relativePath":"Pemrograman/JavaScript/Javascript For Frontend/Onload.md","folder":"Pemrograman/JavaScript/Javascript For Frontend","frontmatter":{"tags":[],"aliases":[]},"tags":[],"aliases":[],"headings":[],"content":"#programming \ntugas pertama kita adalah memunculkan semua konten HTML setelah semua baris pada berkas HTML diproses oleh browser di mana event onload akan terjadi. Pada bagian tag `<script>,` kita akan menulis kode perintah, yang mana akan dijalankan ketika event onload terjadi pada elemen body. Kode perintah tersebut disebut sebagai event handler dari event onload.\n\nBagaimana membuat sebuah event handler untuk elemen tertentu? Tenang, kita akan membahas satu per satu dengan dimulai dari membuat sebuah fungsi event handler hingga “mengaitkan” fungsi tersebut supaya berjalan ketika event onload terjadi pada elemen body.\n\nCobalah buka berkas HTML dan deklarasikan function baru bernama welcome yang akan menghapus atribut hidden pada elemen `<div>` ketika elemen body selesai diproses oleh browser lalu menjalankan event onload. Fungsi tersebut diletakkan di antara tag `<script>`.\n```js\nfunction welcome(){\n\talert('Sim salabim muncullah elemen-elemen HTML!');\n\tconst konten = document.querySelector('.contents');\n\tkonten.removeAttribute('hidden');\n}\n```\n\nPada kode di atas, kita akan memunculkan sebuah dialog box alert. Fungsi dari dialog box adalah memberitahu user bahwa elemen HTML yang sebelumnya bersembunyi akan ditampilkan. Caranya adalah dengan menghilangkan atribut hidden dari elemen `<div>`. Untuk menghilangkan atribut hidden, gunakan method removeAttribute() dari elemen yang dituju.\n\nMendekati akhir dari penutup tag `</script>`, kita perlu menambah kode event handler untuk event onload dari elemen body. Pada umumnya, menambahkan sebuah event handler pada sebuah event yang berasal dari sebuah elemen HTML tertentu akan menggunakan pendekatan seperti berikut.\n```js\nelemen.{nama_event} = namaFunction();\n```\n\njika di implementasikan ke dalam code yang sudah dibuat, maka akan seperti ini:\n```js\ndocument.body.onload = welcome;\n```\nPada kasus menulis event handler onload di sini, kode yang digunakan untuk memberikan event pada elemen body adalah sebagai berikut.\n\ncode lengkapnya untuk event onload seperti ini:\n```js\n<script>\n  function welcome() {\n    alert('Sim salabim muncullah elemen-elemen HTML!');\n    const konten = document.querySelector('.contents');\n    konten.removeAttribute('hidden');\n  }\n  document.body.onload = welcome;\n</script>\n```\n\nJika kita jalankan lagi melalui browser, maka akan muncul alert dengan pesan “sim salabim muncullah elemen-elemen HTML”.\n\n","excerpt":"#programming  tugas pertama kita adalah memunculkan semua konten HTML setelah semua baris pada berkas HTML diproses oleh browser di mana event onload akan terjadi. Pada bagian tag `<script>,` kita akan menulis kode perintah, yang mana akan","readingTime":2,"updated":null,"contentHash":"sha256:0d0c0369dabba76f0b04b99df76f3aff9f04730b9f1f15054cb8185e546e462e","outputPath":"Pemrograman/JavaScript/Javascript For Frontend/Onload","breadcrumb":["Pemrograman","JavaScript","Javascript For Frontend"],"links":[],"backlinks":[{"id":"pemrograman/javascript/javascript-for-frontend/03_javascript-for-frontend","title":"03_Javascript For Frontend"}],"brokenLinks":[],"html":"<p>#programming\ntugas pertama kita adalah memunculkan semua konten HTML setelah semua baris pada berkas HTML diproses oleh browser di mana event onload akan terjadi. Pada bagian tag <code>&#x3C;script>,</code> kita akan menulis kode perintah, yang mana akan dijalankan ketika event onload terjadi pada elemen body. Kode perintah tersebut disebut sebagai event handler dari event onload.</p>\n<p>Bagaimana membuat sebuah event handler untuk elemen tertentu? Tenang, kita akan membahas satu per satu dengan dimulai dari membuat sebuah fungsi event handler hingga “mengaitkan” fungsi tersebut supaya berjalan ketika event onload terjadi pada elemen body.</p>\n<p>Cobalah buka berkas HTML dan deklarasikan function baru bernama welcome yang akan menghapus atribut hidden pada elemen <code>&#x3C;div></code> ketika elemen body selesai diproses oleh browser lalu menjalankan event onload. Fungsi tersebut diletakkan di antara tag <code>&#x3C;script></code>.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">welcome</span>(<span class=\"hljs-params\"></span>){\n\t<span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">'Sim salabim muncullah elemen-elemen HTML!'</span>);\n\t<span class=\"hljs-keyword\">const</span> konten = <span class=\"hljs-variable language_\">document</span>.<span class=\"hljs-title function_\">querySelector</span>(<span class=\"hljs-string\">'.contents'</span>);\n\tkonten.<span class=\"hljs-title function_\">removeAttribute</span>(<span class=\"hljs-string\">'hidden'</span>);\n}\n</code></pre>\n<p>Pada kode di atas, kita akan memunculkan sebuah dialog box alert. Fungsi dari dialog box adalah memberitahu user bahwa elemen HTML yang sebelumnya bersembunyi akan ditampilkan. Caranya adalah dengan menghilangkan atribut hidden dari elemen <code>&#x3C;div></code>. Untuk menghilangkan atribut hidden, gunakan method removeAttribute() dari elemen yang dituju.</p>\n<p>Mendekati akhir dari penutup tag <code>&#x3C;/script></code>, kita perlu menambah kode event handler untuk event onload dari elemen body. Pada umumnya, menambahkan sebuah event handler pada sebuah event yang berasal dari sebuah elemen HTML tertentu akan menggunakan pendekatan seperti berikut.</p>\n<pre><code class=\"hljs language-js\">elemen.{nama_event} = <span class=\"hljs-title function_\">namaFunction</span>();\n</code></pre>\n<p>jika di implementasikan ke dalam code yang sudah dibuat, maka akan seperti ini:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-variable language_\">document</span>.<span class=\"hljs-property\">body</span>.<span class=\"hljs-property\">onload</span> = welcome;\n</code></pre>\n<p>Pada kasus menulis event handler onload di sini, kode yang digunakan untuk memberikan event pada elemen body adalah sebagai berikut.</p>\n<p>code lengkapnya untuk event onload seperti ini:</p>\n<pre><code class=\"hljs language-js\">&#x3C;script>\n  <span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">welcome</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">'Sim salabim muncullah elemen-elemen HTML!'</span>);\n    <span class=\"hljs-keyword\">const</span> konten = <span class=\"hljs-variable language_\">document</span>.<span class=\"hljs-title function_\">querySelector</span>(<span class=\"hljs-string\">'.contents'</span>);\n    konten.<span class=\"hljs-title function_\">removeAttribute</span>(<span class=\"hljs-string\">'hidden'</span>);\n  }\n  <span class=\"hljs-variable language_\">document</span>.<span class=\"hljs-property\">body</span>.<span class=\"hljs-property\">onload</span> = welcome;\n&#x3C;/script>\n</code></pre>\n<p>Jika kita jalankan lagi melalui browser, maka akan muncul alert dengan pesan “sim salabim muncullah elemen-elemen HTML”.</p>","previous":"pemrograman/javascript/javascript-for-frontend/oninput,-onfocus,-dan-onblur","next":"pemrograman/javascript/javascript-for-frontend/pendahulan"}