{"id":"devops/docker/implementasi-container","title":"Implementasi Container","slug":"implementasi container","relativePath":"DevOps/Docker/Implementasi Container.md","folder":"DevOps/Docker","frontmatter":{"tags":[],"aliases":[]},"tags":["devops"],"aliases":[],"headings":[{"depth":1,"text":"Membuat Container","id":"membuat-container"},{"depth":1,"text":"Menjalankan Container","id":"menjalankan-container"},{"depth":1,"text":"Menghapus Container","id":"menghapus-container"},{"depth":1,"text":"Membuka Port Container","id":"membuka-port-container"}],"content":"#devops\n# Membuat Container\n1. untuk melihat daftar container yang ada di local kita yang sedang berjalan (running), bisa menggunakan perintah\n```shell\ndocker container ls\n```\nkarna masih kosong maka outputnya adalah\n```\nCONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES\n```\n\n2. dan untuk melihat container yang ada di local walaupun sedang tidak jalan bisa menggunakan perintah\n```shell\ndocker container ls --all\n```\nartinya dia akan menampilkan semua container yang running maupun tidak.\nkarna belum ada 1 pun container dilaptopku maka hasilnya sama seperti diatas itu.\n\n3. dan sekarang akan membuat container dari image yang diinstall dari docker hub tadi:\n```shell\ndocker container create <image>:tag\n```\n\n4. secara default docker akan memberikan random name terhadap container tersebut, dan bisa menyulitkan kalo mau menjalankan containernya karna kita harus menyebutkan nama dari container itu.\n\nagar tidak random name, kita bisa menambahkan nama container:\n```shell\ndocker container create --name FirstContainer <image>:tag\n```\n\n5. seperti yang sudah di jelaskan sebelumnya kalo kita bisa membuat lebih dari 1 container dari 1 image, tapi kita tidak bisa menggunakan nama yang sama, karena nama di container ini bersifat uniqe atau tidak boleh sama dengan yang lain. \n\n# Menjalankan Container\n1. Aku sudah membuat docker containernya, sekarang akan menjalankannya dengan perintah:\n```docker\ndocker container start FirstContainer\n```\n\"FirstContainer\" disitu adalah nama dari container yang ingin dijalankan itu, \nkalo terminal memanggil nama containernya itu berarti sukses berjalan.\n\n2. untuk memastikan container itu sudah jalan atau belum, gunakan perintah:\n```shell\ndocker container ls\n```\nkalo ada namanya berarti sudah fix jalan\n\nsekarang containernya sudah bisa digunakan.\n\n# Menghapus Container\nuntuk menghapus container cukup jalankan command ini:\n```shell\ndocker container rm <NamaContainer> \natau\ndocker container rm FirstContainer\n```\n\ntapi kita tidak bisa menghapus container yang sedang running, jadi sebelum menghapus, pastikan containernya itu sudah dimatikan dulu, gimana caranya mematikannya, gunakan perintah:\n```shell\ndocker container stop <NamaContainer>\n```\natau kita bisa mematikan sekaligus banyak seperti:\n```shell\ndocker container stop <NamaContainer1>, <NamaContainer2>, <NamaContainer3>, ...\n```\n\njika sudah di stop/matikan, maka container itu sudah bisa dihapus dengan perintah tadi:\n```shell\ndocker container rm <NamaContainer>\n```\nbisa sekaligus banyak dengan dipisahkan koma.\n\n>Jangan khawatir saat menghapus container, image yang sudah di install tadi akan tetap ada.\n\n# Membuka Port Container\nContainer yang sudah di jalankan itu belum bisa dipakai untuk luar platform karena itu hanya bisa dipakai di dalam dockernya saja, gimana agar bisa untuk dipakai diluar juga? \nexport port nya atau dibuka terlebih dahulu.\n\nperintahnya kurang lebih sama seperti saat menjalankan, tetapi ada sedikit tambahan untuk bisa diakses dari luar, yaitu:\n```shell\ndocker container create --name FirstContainer -p 8080:27017 <NamaImage>:tag\n```\n27017 = port internal container.\n8080 = port untuk bisa diakses dari luar.\n\nSaat kita membuat container yang lain, kita tidak boleh meletakan port yang sama dengan yang sudah ada untuk port akses keluarnya, contoh:\n```shell\ndocker container create --name SecondContainer -p 8181:27017 <NamaImage>:tag\n```\nkenapa harus berbeda? supaya portnya tidak bentrok dengan yang sudah ada.\ntetapi untuk port internal containernya tetap sama.\n\ndan sekarang kita bisa connect ke platform docker client nya menggunakan port public itu.\n\n\n","excerpt":"#devops # Membuat Container 1. untuk melihat daftar container yang ada di local kita yang sedang berjalan (running), bisa menggunakan perintah ```shell docker container ls ``` karna masih kosong maka outputnya adalah ``` CONTAINER ID   IMAG","readingTime":3,"updated":null,"contentHash":"sha256:fa32753ee75a80a3ede44474d4482ab191d275a2a34b380b2cfe7ed3d9b6a982","outputPath":"DevOps/Docker/Implementasi Container","breadcrumb":["DevOps","Docker"],"links":[],"backlinks":[{"id":"devops/docker/docker","title":"Docker"}],"brokenLinks":[],"html":"<h1 id=\"membuat-container\">Membuat Container</h1>\n<ol>\n<li>untuk melihat daftar container yang ada di local kita yang sedang berjalan (running), bisa menggunakan perintah</li>\n</ol>\n<pre><code class=\"hljs language-shell\">docker container ls\n</code></pre>\n<p>karna masih kosong maka outputnya adalah</p>\n<pre><code>CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES\n</code></pre>\n<ol>\n<li>dan untuk melihat container yang ada di local walaupun sedang tidak jalan bisa menggunakan perintah</li>\n</ol>\n<pre><code class=\"hljs language-shell\">docker container ls --all\n</code></pre>\n<p>artinya dia akan menampilkan semua container yang running maupun tidak.\nkarna belum ada 1 pun container dilaptopku maka hasilnya sama seperti diatas itu.</p>\n<ol>\n<li>dan sekarang akan membuat container dari image yang diinstall dari docker hub tadi:</li>\n</ol>\n<pre><code class=\"hljs language-shell\">docker container create &#x3C;image>:tag\n</code></pre>\n<ol>\n<li>secara default docker akan memberikan random name terhadap container tersebut, dan bisa menyulitkan kalo mau menjalankan containernya karna kita harus menyebutkan nama dari container itu.</li>\n</ol>\n<p>agar tidak random name, kita bisa menambahkan nama container:</p>\n<pre><code class=\"hljs language-shell\">docker container create --name FirstContainer &#x3C;image>:tag\n</code></pre>\n<ol>\n<li>seperti yang sudah di jelaskan sebelumnya kalo kita bisa membuat lebih dari 1 container dari 1 image, tapi kita tidak bisa menggunakan nama yang sama, karena nama di container ini bersifat uniqe atau tidak boleh sama dengan yang lain.</li>\n</ol>\n<h1 id=\"menjalankan-container\">Menjalankan Container</h1>\n<ol>\n<li>Aku sudah membuat docker containernya, sekarang akan menjalankannya dengan perintah:</li>\n</ol>\n<pre><code class=\"hljs language-docker\">docker container start FirstContainer\n</code></pre>\n<p>\"FirstContainer\" disitu adalah nama dari container yang ingin dijalankan itu,\nkalo terminal memanggil nama containernya itu berarti sukses berjalan.</p>\n<ol>\n<li>untuk memastikan container itu sudah jalan atau belum, gunakan perintah:</li>\n</ol>\n<pre><code class=\"hljs language-shell\">docker container ls\n</code></pre>\n<p>kalo ada namanya berarti sudah fix jalan</p>\n<p>sekarang containernya sudah bisa digunakan.</p>\n<h1 id=\"menghapus-container\">Menghapus Container</h1>\n<p>untuk menghapus container cukup jalankan command ini:</p>\n<pre><code class=\"hljs language-shell\">docker container rm &#x3C;NamaContainer> \natau\ndocker container rm FirstContainer\n</code></pre>\n<p>tapi kita tidak bisa menghapus container yang sedang running, jadi sebelum menghapus, pastikan containernya itu sudah dimatikan dulu, gimana caranya mematikannya, gunakan perintah:</p>\n<pre><code class=\"hljs language-shell\">docker container stop &#x3C;NamaContainer>\n</code></pre>\n<p>atau kita bisa mematikan sekaligus banyak seperti:</p>\n<pre><code class=\"hljs language-shell\">docker container stop &#x3C;NamaContainer1>, &#x3C;NamaContainer2>, &#x3C;NamaContainer3>, ...\n</code></pre>\n<p>jika sudah di stop/matikan, maka container itu sudah bisa dihapus dengan perintah tadi:</p>\n<pre><code class=\"hljs language-shell\">docker container rm &#x3C;NamaContainer>\n</code></pre>\n<p>bisa sekaligus banyak dengan dipisahkan koma.</p>\n<blockquote>\n<p>Jangan khawatir saat menghapus container, image yang sudah di install tadi akan tetap ada.</p>\n</blockquote>\n<h1 id=\"membuka-port-container\">Membuka Port Container</h1>\n<p>Container yang sudah di jalankan itu belum bisa dipakai untuk luar platform karena itu hanya bisa dipakai di dalam dockernya saja, gimana agar bisa untuk dipakai diluar juga?\nexport port nya atau dibuka terlebih dahulu.</p>\n<p>perintahnya kurang lebih sama seperti saat menjalankan, tetapi ada sedikit tambahan untuk bisa diakses dari luar, yaitu:</p>\n<pre><code class=\"hljs language-shell\">docker container create --name FirstContainer -p 8080:27017 &#x3C;NamaImage>:tag\n</code></pre>\n<p>27017 = port internal container.\n8080 = port untuk bisa diakses dari luar.</p>\n<p>Saat kita membuat container yang lain, kita tidak boleh meletakan port yang sama dengan yang sudah ada untuk port akses keluarnya, contoh:</p>\n<pre><code class=\"hljs language-shell\">docker container create --name SecondContainer -p 8181:27017 &#x3C;NamaImage>:tag\n</code></pre>\n<p>kenapa harus berbeda? supaya portnya tidak bentrok dengan yang sudah ada.\ntetapi untuk port internal containernya tetap sama.</p>\n<p>dan sekarang kita bisa connect ke platform docker client nya menggunakan port public itu.</p>","previous":"devops/docker/image","next":"devops/docker/integrasi-container-dengan-network"}