{"id":"devops/docker/docker-basic/menghapus-docker-image","title":"Menghapus Docker Image","slug":"menghapus docker image","relativePath":"DevOps/Docker/Docker Basic/Menghapus Docker Image.md","folder":"DevOps/Docker/Docker Basic","frontmatter":{"tags":[],"aliases":[]},"tags":["devops"],"aliases":[],"headings":[],"content":"#devops \nuntuk menghapus docker image yang kita punya, kita bisa menggunakan perintah\n```shell\ndocker image rm <NamaImage>:tag\n```\n\ntapi terkadang saat menghapus, kita mendapatkan error conflict karena ada salah satu container yang memakai image tersebut, contoh errornya seperti ini:\n![](Pasted%20image%2020260724203413.png)\n\njika mendapati error seperti itu, kita harus menghapus container yang mengandung image itu terlebih dahulu, dengan cara yang sudah di sebutkan sebelumnya:\n```shell\ndocker container stop <NamaContainer>  \ndocker container rm <NamaContainer>  \n```\n\njika sudah, maka kita sudah bisa menghapus image tersebut.","excerpt":"#devops  untuk menghapus docker image yang kita punya, kita bisa menggunakan perintah ```shell docker image rm <NamaImage>:tag ```","readingTime":1,"updated":null,"contentHash":"sha256:7d7bcb4d3690593a75bf4da613d9a20a63cf75e17350cd9774a9775841903df4","outputPath":"DevOps/Docker/Docker Basic/Menghapus Docker Image","breadcrumb":["DevOps","Docker","Docker Basic"],"links":[],"backlinks":[{"id":"devops/docker/docker-basic/1.-docker-basic","title":"1. Docker Basic"}],"brokenLinks":[],"html":"<p>\nuntuk menghapus docker image yang kita punya, kita bisa menggunakan perintah</p>\n<pre><code class=\"hljs language-shell\">docker image rm &#x3C;NamaImage>:tag\n</code></pre>\n<p>tapi terkadang saat menghapus, kita mendapatkan error conflict karena ada salah satu container yang memakai image tersebut, contoh errornya seperti ini:\n<img src=\"Pasted%20image%2020260724203413.png\" alt=\"\"></p>\n<p>jika mendapati error seperti itu, kita harus menghapus container yang mengandung image itu terlebih dahulu, dengan cara yang sudah di sebutkan sebelumnya:</p>\n<pre><code class=\"hljs language-shell\">docker container stop &#x3C;NamaContainer>  \ndocker container rm &#x3C;NamaContainer>  \n</code></pre>\n<p>jika sudah, maka kita sudah bisa menghapus image tersebut.</p>","previous":"devops/docker/docker-basic/mengambil-image-dari-registry","next":"devops/docker/docker-basic/mengupload-image-ke-registry"}